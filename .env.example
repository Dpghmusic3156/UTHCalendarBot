# Telegram Bot Token (lấy từ @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# Dev code (mã xác thực để dùng lệnh dev như /restart)
DEV_CODE=your_secret_dev_code

# Portal credentials are now per-user (via /login command)
# No need to set them here anymore
